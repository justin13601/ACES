predicates:
  admission:
    code: event_type//ADMISSION
  discharge:
    code: event_type//DISCHARGE

trigger_event: admission

windows:
  input:
    start: trigger
    end: start -> discharge
    start_inclusive: False
    end_inclusive: True
  target:
    start: input.end
    end: start + 30 days
    start_inclusive: False
    end_inclusive: True
    label: admission
